# Changelog

## 0.1.0

- Initial release: MCP router with `stdio` + Streamable HTTP downstream transports
- Config-driven upstream registry (`mcp-router.config.json`)
- Token auth, projects RBAC, rate limiting, audit logs
- Tag routing + semver version routing (beta)
- Circuit breaker + periodic health checks (beta)
- Prometheus metrics and `/healthz`

